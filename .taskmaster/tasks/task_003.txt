# Task ID: 3
# Title: Implement Markdown Parsing and Metadata Extraction
# Status: pending
# Dependencies: 2
# Priority: high
# Description: Create utility functions to parse Markdown files and extract metadata using the TDD approach.
# Details:
Use gray-matter (version 4.0.3) for parsing YAML front matter. Implement functions to read Markdown files from the 'post' folder, extract metadata (title, date, tags, summary), and parse Markdown content. Use remark (version 14.0.3) for Markdown to HTML conversion.

# Test Strategy:
Write tests for metadata extraction, content parsing, and error handling (e.g., missing required fields). Use mock filesystem for testing file reading operations.
